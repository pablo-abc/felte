{
  "name": "@example/webcomponent-custom-field",
  "description": "Example of using Felte with vanilla JS using @felte/element and @felte/reporter-element. Using `<felte-field>` to handle contenteditable divs",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@felte/element": "latest",
    "@felte/reporter-element": "latest"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^2.8.0"
  }
}
